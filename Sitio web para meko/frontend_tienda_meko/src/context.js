/*import {createContext} from "react"
export const UserContext=createContext({
    'login':false,
});

export const CarritoContext = createContext([]);
export const TipoMonedaContext = createContext([]);
*/